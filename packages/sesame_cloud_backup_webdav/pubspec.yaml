name: sesame_cloud_backup_webdav
description: WebDAV provider for sesame_cloud_backup
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  sesame_cloud_backup:
    path: ../sesame_cloud_backup
  webdav_client: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
